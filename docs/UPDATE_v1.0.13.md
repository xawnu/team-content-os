# v1.0.13 更新说明

## 📚 新功能：历史记录增强

### 主要改进

1. **智能筛选器**
   - 按主题关键词筛选
   - 按内容目的筛选（拉新破圈/提升完播/提升互动/承接转化）
   - 实时显示筛选结果数量
   - 一键清除筛选条件

2. **复用配置功能**
   - 每条历史记录新增"复用"按钮
   - 一键恢复历史生成配置
   - 自动填充所有参数（内容目的、叙事结构、表达语气等）
   - 自动滚动到顶部，方便查看和修改

3. **批量操作**
   - 支持多选历史记录（复选框）
   - 全选/取消选择
   - 批量删除（确认后不可恢复）
   - 显示已选数量

4. **数据导出**
   - 导出为 JSON 格式
   - 包含完整配置信息
   - 支持备份和数据分析
   - 文件名自动带时间戳

5. **统计信息**
   - 显示总记录数
   - 显示筛选后记录数
   - 实时更新

### 使用场景

**场景 1：快速找到相似主题的历史配置**
```
1. 在筛选框输入"chicken"
2. 找到所有关于养鸡的历史记录
3. 点击"复用"按钮
4. 配置自动填充，稍作调整后重新生成
```

**场景 2：清理低质量历史记录**
```
1. 筛选出特定类型的记录
2. 勾选不需要的记录
3. 点击"批量删除"
4. 确认后一次性删除
```

**场景 3：备份历史数据**
```
1. 点击"导出 JSON"按钮
2. 下载包含所有配置的 JSON 文件
3. 保存到本地或云端
4. 需要时可以查看历史配置
```

**场景 4：分析内容策略**
```
1. 按内容目的筛选（如"拉新破圈"）
2. 导出 JSON 数据
3. 分析哪些配置效果最好
4. 优化未来的内容策略
```

### UI 变化

**历史记录表头：**
```
之前：
┌─────────────────────────────────────┐
│ 历史选题（可作为补充灵感）           │
├─────────────────────────────────────┤
│ 主题 | 关键词 | 日期 | 标题 | 操作  │
└─────────────────────────────────────┘

现在：
┌─────────────────────────────────────┐
│ 历史选题（可作为补充灵感）           │
│ [📥 导出 JSON] [🗑️ 批量删除 (3)]    │
│ [按主题筛选...] [全部目的▼] [清除]  │
│ [全选] [取消选择]                    │
│ 共 50 条记录，筛选后 12 条           │
├─────────────────────────────────────┤
│ ☑ | 主题 | 关键词 | 日期 | 标题 | 操作│
│ ☑ | ...  | ...    | ...  | ...  | 复用│
└─────────────────────────────────────┘
```

**操作按钮：**
```
之前：[查看] [删除]

现在：[复用] [查看] [删除]
```

### 功能详解

#### 1. 筛选器

**主题筛选：**
- 输入关键词，实时过滤
- 不区分大小写
- 支持部分匹配

**内容目的筛选：**
- 下拉选择
- 选项：全部目的、拉新破圈、提升完播、提升互动、承接转化
- 只显示匹配的记录

**清除筛选：**
- 一键清空所有筛选条件
- 恢复显示全部记录

#### 2. 复用配置

**复用的参数：**
- 内容目的（contentGoal）
- 叙事结构（narrativeStructure）
- 表达语气（toneStyle）
- 节奏强度（paceLevel）
- 主题锁定（topicLock）
- 禁用词（bannedWords）
- 参考频道（seedText）
- 输出方向（direction）

**操作流程：**
1. 点击"复用"按钮
2. 系统自动填充所有参数
3. 页面滚动到顶部
4. 弹出提示"配置已复用！"
5. 检查参数，点击"生成详细文案"

#### 3. 批量操作

**选择记录：**
- 单个勾选：点击每行的复选框
- 全选：点击表头的复选框或"全选"按钮
- 取消选择：点击"取消选择"按钮

**批量删除：**
- 选择要删除的记录
- 点击"批量删除 (N)"按钮
- 确认删除
- 删除完成后自动刷新列表

#### 4. 数据导出

**导出内容：**
```json
[
  {
    "主题": "How to Build a Chicken Coop",
    "关键词": "chicken coop",
    "计划日期": "2026-02-20",
    "标题": "5 Steps to Build...",
    "创建时间": "2026-02-20T10:30:00Z",
    "配置": {
      "contentGoal": "拉新破圈",
      "narrativeStructure": "问题→方案→结果",
      "toneStyle": ["专业理性"],
      "paceLevel": "中",
      "topicLock": "chicken coop",
      "bannedWords": "",
      "seedText": "@homesteadrootss",
      "direction": "同类型视频详细文案"
    }
  }
]
```

**文件命名：**
- 格式：`episodes-export-{timestamp}.json`
- 示例：`episodes-export-1740348000000.json`

### 技术实现

**筛选逻辑：**
```typescript
function getFilteredEpisodes() {
  return episodes.filter(e => {
    // 主题筛选
    if (filterTopic && !e.topic.toLowerCase().includes(filterTopic.toLowerCase())) {
      return false;
    }
    // 内容目的筛选
    if (filterGoal) {
      const outline = JSON.parse(e.scriptOutline || '{}');
      if (outline.contentGoal !== filterGoal) return false;
    }
    return true;
  });
}
```

**复用配置：**
```typescript
function reuseEpisodeConfig(episode: Episode) {
  const outline = JSON.parse(episode.scriptOutline || '{}');
  
  // 恢复所有配置
  if (outline.contentGoal) setContentGoal(outline.contentGoal);
  if (outline.narrativeStructure) setNarrativeStructure(outline.narrativeStructure);
  // ... 其他参数
  
  // 滚动到顶部
  window.scrollTo({ top: 0, behavior: 'smooth' });
}
```

**批量删除：**
```typescript
async function batchDeleteEpisodes() {
  for (const id of selectedEpisodes) {
    await fetch("/api/planner/episodes", {
      method: "DELETE",
      body: JSON.stringify({ episodeId: id }),
    });
  }
  clearSelection();
  await loadEpisodes();
}
```

### 用户体验提升

**之前：**
- 历史记录只能逐条查看
- 无法快速找到相似主题
- 重新生成需要手动填写所有参数
- 删除只能一条条操作
- 无法导出备份

**现在：**
- 智能筛选，快速定位
- 一键复用配置
- 批量操作，提高效率
- 支持数据导出和备份

### 注意事项

1. **复用配置后需检查**
   - 复用的配置可能不完全适合当前需求
   - 建议检查所有参数后再生成
   - 特别注意参考视频池是否需要更新

2. **批量删除不可恢复**
   - 删除前请确认
   - 建议先导出备份
   - 删除后无法撤销

3. **导出文件格式**
   - JSON 格式，可用文本编辑器打开
   - 包含完整配置信息
   - 可用于数据分析或迁移

4. **筛选器性能**
   - 前端筛选，实时响应
   - 大量记录时可能略有延迟
   - 建议定期清理旧记录

### 下一步优化

- [ ] 支持按评分范围筛选
- [ ] 支持按创建时间范围筛选
- [ ] 支持导出 Markdown 格式
- [ ] 支持导入 JSON 配置
- [ ] 支持历史记录标签分类

---

**部署说明：**

1. 拉取最新代码：`git pull origin master`
2. 重新构建：`npm run build`
3. 重启服务：`pm2 restart youtube.9180.net`

**兼容性：**
- 向后兼容，无破坏性变更
- 旧的历史记录可以正常筛选和复用
- 新功能不影响现有功能
