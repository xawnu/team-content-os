DATABASE_URL="postgresql://user:pass@localhost:5432/team_content_os"

# 随机长字符串（建议 32+ 字符）
AUTH_SECRET="replace-with-a-random-secret"

# 仅允许这些邮箱登录，密码请用 bcrypt 哈希
# 生成哈希示例：node -e "console.log(require('bcryptjs').hashSync('your_password', 10))"
AUTH_USERS_JSON='[{"email":"amber@example.com","passwordHash":"$2b$10$replace_me","role":"admin"}]'
